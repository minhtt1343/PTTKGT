<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="mazesolver2.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:mazesolver2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Maze Solver">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel
            Grid.Row="0"
            Orientation="Horizontal"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="10">

            <Button
                Content="Maze 1"
                Click="Maze1_Click"/>

            <Button
                Content="Maze 3"
                Click="Maze3_Click"/>

            <Button
                Content="Maze 4"
                Click="Maze4_Click"/>

            <Button
                Content="Maze 5"
                Click="Maze5_Click"/>

            <ComboBox x:Name="AlgoComboBox" SelectedIndex="0" VerticalAlignment="Center">
                <ComboBoxItem Content="Backtracking"/>
                <ComboBoxItem Content="BFS"/>
            </ComboBox>

            <Button
                Content="Find Path"
                Click="FindPath_Click"/>

            <Button
                Content="Reset"
                Click="ResetButton_Click"/>

            <Button
                Content="Set Start"
                Click="SetStartButton_Click"/>

            <Button
                Content="Set End"
                Click="SetEndButton_Click"/>

        </StackPanel>

        <Grid
            x:Name="MazeGrid"
            Grid.Row="1"
            Margin="20"/>
    </Grid>
</Window>